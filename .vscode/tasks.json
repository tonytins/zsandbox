{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "zig run tokenize",
            "type": "shell",
            "command": "zig build run -- --tokens"
        },
        {
            "label": "zig run cuboid",
            "type": "shell",
            "command": "zig build run -- --cuboid"
        },
        
        {
            "label": "zig run chiper",
            "type": "shell",
            "command": "zig build run -- --chiper"
        }
    ]
}