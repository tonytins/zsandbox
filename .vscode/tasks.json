{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "zig run FPS",
            "type": "shell",
            "command": "cd fps && zig build run"
        },
        {
            "label": "zig run screen manger",
            "type": "shell",
            "command": "cd scrnmanger && zig build run"
        }
    ]
}